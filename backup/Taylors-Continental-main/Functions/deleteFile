import os
import glob

folderDir = 'C:/Users/Jae/Desktop/Mongo/testPic/*'

def removePicInFolder():
    picDir = glob.glob(folderDir)
    for pic in picDir:
        os.remove(pic)

removePicInFolder()